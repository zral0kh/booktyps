[package]
name = "booktyps"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["zral0kh"]
license = "MIT"
description = "The booktabs look for Typst tables, read off the table's own structure."
repository = "https://github.com/zral0kh/booktyps"
keywords = ["table", "booktabs", "rules", "stroke"]
categories = ["components", "layout"]
compiler = "0.11.0"
exclude = ["tests"]

[tool.tytanic]
default.ppi = 144
